import { PatternConsumer } from "src/app/modules/base/pattern/pattern.consumer";
import { ViewRegionComponent } from "../../pattern/view-region";
import { ModelRegionDirective } from "../../pattern/model-region";

export class AccordionConsumer extends PatternConsumer {
    
    override onAction(action: string, event: any): void {
        // TODO:
    }
    
}