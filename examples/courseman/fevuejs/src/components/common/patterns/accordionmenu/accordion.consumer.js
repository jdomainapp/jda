import PatternConsumer from "../../pattern/pattern.consumer";

export default class AccordionConsumer extends PatternConsumer {
    onAction(action, event) {
        action;
        event;
        // Todo:
    }
}
